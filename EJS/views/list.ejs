<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>LIST</title>
  </head>
  <body>
    <h1><%= title%></h1>
    <h2>Current seconds: seconds</h2>
    <% if (seconds % 2 ===0) { %>
    <ul>
      <% items.forEach(item => { %>
      <li><%= item %></li>
      <% }); %>
    </ul>
    <% } else { %>
    <p>No items to display</p>
    <% } %> <%- htmlContent %>
  </body>
  <footer>
    <p>Copyright © from footer</p>
  </footer>
</html>
